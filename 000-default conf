<VirtualHost *:80>
	# The ServerName directive sets the request scheme, hostname and port that
	# the server uses to identify itself. This is used when creating
	# redirection URLs. In the context of virtual hosts, the ServerName
	# specifies what hostname must appear in the request's Host: header to
	# match this virtual host. For the default virtual host (this file) this
	# value is not decisive as it is used as a last resort host regardless.
	# However, you must set it for any further virtual host explicitly.
	#ServerName www.example.com

	ServerAdmin webmaster@localhost
	DocumentRoot /var/www/html

	<Directory /var/www/html/>
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>

	# Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
	# error, crit, alert, emerg.
	# It is also possible to configure the loglevel for particular
	# modules, e.g.
	#LogLevel info ssl:warn

	ErrorLog ${APACHE_LOG_DIR}/error.log
	CustomLog ${APACHE_LOG_DIR}/access.log combined

	# For most configuration files from conf-available/, which are
	# enabled or disabled at a global level, it is possible to
	# include a line for only one particular virtual host. For example the
	# following line enables the CGI configuration for this host only
	# after it has been globally disabled with "a2disconf".
	#Include conf-available/serve-cgi-bin.conf
</VirtualHost>




# b2b-inventory.test
Define server_name b2b-inventory.test
Define server_path /var/www/html/b2b-inventory/public/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>

# b2b-wholesale.test
Define server_name b2b-wholesale.test
Define server_path /var/www/html/b2b-inventory-wholesale/public/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>

# mingle.test
Define server_name mingle.test
Define server_path /var/www/html/mingle-inventory/public/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>


# canteen.test
Define server_name canteen.test
Define server_path /var/www/html/canteen-inventory/public/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>


# pbc.test
Define server_name pbc.test
Define server_path /var/www/html/pickleball-cafe/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>



# pbc.test
Define server_name new.aperally.com.test
Define server_path /var/www/html/new.aperally.com/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>


# olfat.test
Define server_name olfat.test
Define server_path /var/www/html/olfat-shipping/public/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>





# olfat.test
Define server_name smp.test
Define server_path /var/www/html/smp/public/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>


# mingle_new.test
Define server_name mingle_new.test
Define server_path /var/www/html/admin/public/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>


# h_shop.test
Define server_name h_shop.test
Define server_path /var/www/html/hamim_shop/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>


# btc.test
Define server_name btc.test
Define server_path /var/www/html/btcnations/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>

# btc.test
Define server_name rolo_old.test
Define server_path /var/www/html/rolo_old/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>


# gpos.test
Define server_name gpos.test
Define server_path /var/www/html/POS/public/
<VirtualHost *:80>
ServerName ${server_name}
ServerAlias ${server_name}
ServerAdmin webmaster@localhost
DocumentRoot ${server_path}

ErrorLog ${APACHE_LOG_DIR}/error.log
CustomLog ${APACHE_LOG_DIR}/access.log combined

<FilesMatch \.php$>
# Apache 2.4.10+ can proxy to unix socket
  #SetHandler "proxy:unix:/var/run/php/php5.6-fpm.sock|fcgi://localhost/" 
  </FilesMatch>

<Directory "${server_path}">
allow from all
Options Indexes FollowSymLinks
AllowOverride All
Require all granted
</Directory>
</VirtualHost>

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
